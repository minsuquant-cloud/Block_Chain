"""자동매매 실행기 (미구현).

바이낸스 API 키가 준비되면 여기에 주문 로직을 구현한다.
- API 키/시크릿은 환경변수(BINANCE_API_KEY, BINANCE_API_SECRET)로 관리 (코드에 하드코딩 금지)
- 먼저 테스트넷(https://testnet.binance.vision)으로 검증 후 실계좌 전환
"""


def place_order(*args, **kwargs):
    raise NotImplementedError("API 키 등록 후 구현 예정입니다. (테스트넷부터 시작)")
